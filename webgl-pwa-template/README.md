# WebGL PWA Template

1. Build Unity WebGL output into `game/Build/`.
2. Ensure output file names match:
   - `game.loader.js`
   - `game.framework.js`
   - `game.data`
   - `game.wasm`
3. Host `game/` over HTTPS.
4. Open `index.html` and verify service worker installs and PWA manifest is valid.
