# Unity Migration Scaffold

This folder contains a Unity-oriented project skeleton for migrating the current browser game into a Unity WebGL + PWA architecture.

- Place this structure under a Unity project's `Assets/` folder.
- Use the scripts as starter templates.
- Configure scenes listed in `Assets/Scenes/`.
